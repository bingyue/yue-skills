# Review 2026-05-19

## Findings

1. `agents/interface.yaml` did not satisfy the portable interface contract used by `yao-meta-skill/scripts/validate_skill.py`.
2. `manifest.json` missed lifecycle fields: `updated_at`, `status`, `maturity_tier`, and `review_cadence`.
3. The renderer passed current examples but could silently pass zero-byte files because the existence check did not require non-empty files.
4. The renderer did not explicitly require PDF preview generation before the right-edge overflow scan.
5. Source-index compaction was Chinese-header-only; English or underscore-style source headers would not map reliably.
6. Wider fact-card tables were documented as disallowed but not automatically compacted.

## Repairs

- Rebuilt `agents/interface.yaml` with `interface` and `compatibility` sections.
- Added lifecycle fields to `manifest.json`.
- Added output-risk and artifact-design profiles.
- Hardened `scripts/render_four_format.py`:
  - file checks require non-empty output
  - PDF preview generation is a required quality check
  - source-index headers support Chinese and English variants
  - wide fact-card tables compact to the five-column visual contract

## Verification

- `python3 yao-meta-skill/scripts/validate_skill.py skills/yao-geo-knowledge-base-builder`
- `python3 scripts/validate_repository.py`
- HubSpot four-format render with `quality-report.json`

## 2026-05-20 Architecture Upgrade

The skill was aligned with the local `ai-brand-kb-builder` source workspace before publication.

Added requirements:

- A systematic structured knowledge-base body before the GEO reuse layer.
- A mandatory complete entity inventory.
- Quality checks for required report sections.
- A reference architecture that separates entity inventory, fact cards, product matrix, prompt pack, and audit source index.

## 2026-05-21 Completeness And HTML Upgrade

User requested system/detail/completeness-first analysis and report generation, plus a sticky menu in the HTML visual report.

Repairs:

- Added authoritative reference framework based on Schema.org, W3C PROV-O, W3C SKOS, Google Search Central, and `ai-brand-kb-builder`.
- Added analysis completeness rubric.
- Added required report section for methodology reference alignment and analysis completeness self-check.
- Added quality checks for the completeness section and HTML sticky menu.
- Updated HTML renderer with a sticky table-of-contents menu for screen reading; the menu is hidden in PDF/print output.
- Re-rendered the HubSpot sample report.

## 2026-05-21 Data Access And Kami Layout Upgrade

User requested capability iteration based on current limitations, kami-style report polishing, and regenerated four-format examples.

Repairs:

- Added `source-acquisition-and-freshness.md` to define public web, user-provided file, authenticated workspace, manual brief, and unavailable-source modes.
- Added mandatory `真实数据获取与限制` report section and quality gate.
- Updated the renderer with kami-inspired editorial tokens while preserving this skill's white-background requirement.
- Added HTML quality checks for editorial tokens and print-mode menu hiding.
- Re-rendered the HubSpot sample report after refreshing official-source facts.
